<%= form_with(model: message) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(specialist.errors.count, "error") %> prohibited this specialist from being saved:</h2>

      <ul>
      <% message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= hidden_field_tag 'foreign', @foreign %>
	<h3 class="wiad-podtytul">Nowa wiadomość</h3>
	<div class="show-wiad row">
		<div class="col-md-4" style="border-right: 1px solid rgba(0, 0, 0, 0.125);">
		  <div class="field">
			<strong>Do:</strong> <%= @receiver.full_name %>
		  </div>

			<%= form.hidden_field :sender_id, :value => current_user.id, :readonly => true %>
			<%= form.hidden_field :receiver_id, :value => @receiver.id, :readonly => true %>

		  <div class="field new-mess">
			<strong>Temat: </strong><%= form.text_field :topic, placeholder: "Podaj temat wiadomości" %>
		  </div>
		</div>
		<div class="col-md-8">
		  <div class="field wiadomosc-tresc">
			<%= form.text_area :message, placeholder: "Treść wiadomości" %>
		  </div>

		  <div class="actions" style="text-align:right;">
			<%= form.submit %>
		  </div>
		</div>
	</div>
<% end %>
